/**
 * @deprecated المشروع يستخدم PostgreSQL عبر `db.ts`.
 * أعد التصدير من الوحدة الحالية فقط — لا تستخدم `node:sqlite` هنا.
 */
export { TEMP_IMPORT_BATCH_TITLE, autoAssignBatchesFromAreas } from "./batchFromAreas.js";
